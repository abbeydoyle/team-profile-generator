// employee parent class
// TODO: name, id, email, getName(), getID(), getEmail(), getRole()

class employee {
      constructor(name, id, email) {
            this.name = name;
            this.id = id;
            this.email = email;
      }
}